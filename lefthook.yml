pre-commit:
  commands:
    oxfmt:
      glob: "**/*.ts"
      run: bun run format ${staged_files}
      stage_fixed: true
    oxlint:
      glob: "**/*.ts"
      run: bun run lint:fix {staged_files}
      stage_fixed: true
